import { SyncpadNavbar } from "@/components/syncpad-navbar";
import SearchBar from "./search-bar";

export const Navbar = () => {
  return (
    <SyncpadNavbar>
      <SearchBar />
    </SyncpadNavbar>
  );
};
